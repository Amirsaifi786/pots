# Farmetos static homepage

Open `index.html` directly, or serve this folder with Apache/XAMPP. The page is a standalone static recreation of the Farmetos homepage using local images in `assets/images`, custom CSS, and vanilla JavaScript.

The collection and contact calls are front-end navigation/phone links only; a static export has no WooCommerce cart, product-detail inventory, or form-processing backend.
