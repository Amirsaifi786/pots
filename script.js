document.head.insertAdjacentHTML('beforeend','<style>[hidden]{display:none!important}</style>');const menu=document.querySelector('.menu-toggle'),nav=document.querySelector('.main-nav');menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)});document.querySelectorAll('.main-nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));const search=document.querySelector('.search-panel');document.querySelector('.search-toggle').onclick=()=>{search.hidden=false;search.querySelector('input').focus()};search.querySelector('button').onclick=()=>search.hidden=true;search.onclick=e=>{if(e.target===search)search.hidden=true};const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>observer.observe(e));document.querySelectorAll('.farm-form').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();form.querySelector('.form-message').textContent='Thank you — your enquiry has been received.'}));document.querySelectorAll('.filter').forEach(button=>button.onclick=()=>{document.querySelectorAll('.filter').forEach(b=>b.classList.toggle('active',b===button));document.querySelectorAll('.product-item').forEach(item=>item.hidden=button.dataset.filter!=='all'&&item.dataset.type!==button.dataset.filter)});
